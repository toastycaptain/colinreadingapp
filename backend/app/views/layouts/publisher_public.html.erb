<!DOCTYPE html>
<html>
  <head>
    <title>Publisher Login</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <style>
      body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; background: #e2e8f0; margin: 0; }
      .wrap { max-width: 420px; margin: 80px auto; background: #fff; border-radius: 12px; padding: 24px; box-shadow: 0 12px 28px rgba(15, 23, 42, 0.12); }
      .field { margin-bottom: 12px; }
      input[type=email], input[type=password] { width: 100%; padding: 10px; border: 1px solid #cbd5e1; border-radius: 8px; }
      input[type=submit] { background: #2563eb; color: #fff; border: none; border-radius: 8px; padding: 10px 14px; cursor: pointer; }
      .flash { margin-bottom: 12px; color: #991b1b; }
    </style>
  </head>
  <body>
    <div class="wrap">
      <% flash.each do |_type, message| %>
        <div class="flash"><%= message %></div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
